// config.js
const CONFIG = {
    MIN_DURATION: 5,
    MAX_DURATION: 25,
    MAX_SIZE: 25 * 1024 * 1024, // 25 MB
    LINK_EXPIRY_DURATION: 60 * 60 * 1000 // 1 hour in milliseconds
};

module.exports = CONFIG;